#include <stdio.h>

/**
 * Main : This is s program that prints alphabets in lowecase
 * Return : Always 0
 */

int main(void)
{
	int a;
	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar ('\n');
	return (0);
}

